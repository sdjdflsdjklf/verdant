export const TAG_DETAIL_HTML = `<section class="garden-tag-detail">
  <header>
    <h1>{{tag}}</h1>
    <p>{{count}} notes</p>
  </header>
  <div class="garden-card-grid">
    {{noteCards}}
  </div>
</section>`;